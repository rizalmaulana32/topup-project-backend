# Active Context

## Metadata
- target_project: `backend`
- created_at: `2026-08-05T00:00:00+07:00`
- updated_at: `2026-08-26T00:00:00+07:00`
- updated_by: `claude`
- status: `active`
- git_branch: `main`
- git_commit: `6db924e (development/backend's own git repo — this is a separate repo from the scaffold root)`

## Current Position
- active_feature: `topup-affiliate-platform`
- active_slice: `payment gateway migrated from Xendit to Duitku (client's decision). Client registered a real (sandbox) Duitku account 2026-08-26 — createInvoice (payment collection) is now real-verified against it and working (deployed and live, real test transactions paid through the sandbox and confirmed via webhook). Disbursement (affiliate payouts) is coded but confirmed NOT provisioned on this account yet. Separately, the real Provider Top-Up vendor (MomoLive) is now deployed and switched on (PROVIDER_TOP_UP_VENDOR=momo on the server, using MomoLive's sandbox test credentials) - real-verified end-to-end through the live API (check-id for test user 1000007 returns their real MomoLive username).`
- current_status: `All five bootstrap slices complete: customer top-up purchase, affiliate registration/dashboard, superadmin admin panel, commission crediting, and withdrawal payout. Build/lint/41 unit/27 e2e all green.`
- current_gate: `G5`
- resume_safe: `yes`
- activity_log_current_entry: `none`

## What Is Known
- `NestJS 11 backend at development/backend now has the full backend feature set from the founder's SDLC spec except a real Provider Top-Up vendor and any frontend.` — `development/backend/src/`
- `Requirement baseline exists at dev-doc/topup-affiliate-platform/ (charter, BRD, PRD, use-case, userflow, database-diagram), all updated for Xendit.`
- `Local Postgres available via development/backend/docker-compose.yml (docker compose up -d); schema is now owned by real TypeORM migrations (src/migrations/, src/data-source.ts), synchronize is unconditionally false. Staging DB adopted the InitialSchema migration by marking it already-applied (inserted its tracking row directly, matching the existing synchronize-created tables) rather than re-running its up() — see 2026-08-19 entry below.`
- `Checkout is live-verified against the real Xendit test-mode API (real invoice id + checkout-staging.xendit.co URL returned and persisted). Xendit Payout (Disbursement) has only been e2e-tested against a mocked client, never a real call.`
- `Full real payment loop verified end-to-end on staging 2026-08-19: real checkout -> real Xendit test-mode invoice -> founder paid it in Xendit's sandbox -> real webhook hit https://api.yaytopup.my.id/api/v1/webhooks/xendit/invoice, passed the real XENDIT_CALLBACK_TOKEN check, marked the transaction paid, ran mock coin injection. Test product and transaction deleted afterward.`
- `Xendit invoice EXPIRED callback now actually updates payment_status (previously acknowledged and ignored). TransactionsService.markExpired + TopupService.handleInvoiceExpired, wired into XenditWebhookController's EXPIRED branch; never downgrades an already-paid transaction. Deployed to staging by scp'ing the 3 changed files + npm run build + pm2 restart (no full re-upload needed for a change this small).`
- `e2e tests MUST run with --runInBand (already set as the default npm run test:e2e) — running e2e spec files in parallel causes real failures (TypeORM synchronize races, and separate files truncating/reading the same shared dev DB concurrently), not just slowness.`
- project_state_baseline: `development/backend/artifacts/shared/PROJECT_STATE.md, last verified 2026-08-10`

## Decisions Locked
- `Project slug: topup-affiliate-platform` — founder chat 2026-08-05
- `Database: PostgreSQL` — founder chat 2026-08-05
- `Global commission rate: 10%; minimum withdrawal amount: 100000` — founder chat 2026-08-05; now the PlatformSettings default and superadmin-configurable via PATCH /api/v1/admin/settings
- `Provider Top-Up: internal mock service (MockProviderTopUpService) behind ProviderTopUpPort, real vendor undecided` — founder chat 2026-08-05
- `Scope: backend only, frontend explicitly out of scope for now` — founder chat 2026-08-05
- `Payment gateway/payout provider: Xendit (xendit-node v7, Invoice API + Payout/Disbursement API), overriding the source document's Midtrans choice` — founder chat 2026-08-05
- `ORM: TypeORM; auth/config: @nestjs/config + class-validator/class-transformer` — recorded in artifacts/architecture/DECISION_LOG.md 2026-08-05
- `Auth: JWT (access 15m / refresh 7d) via @nestjs/jwt + passport-jwt; bcrypt password hashing` — founder-approved plan 2026-08-10
- `Registration fields for affiliates: name, email, password, bank_name, account_number, account_holder` — founder-approved plan 2026-08-10
- `Superadmin approval endpoint is now real (POST /api/v1/admin/affiliates/:id/approve|reject); superadmin accounts themselves are still provisioned via direct SQL only (no self-registration)` — founder-approved plan 2026-08-10
- `PlatformSettings is a singleton row (id=1) for global commission rate / minimum withdrawal; per-affiliate override stays on AffiliatorProfile.commission_rate` — recorded in DECISION_LOG.md 2026-08-10
- `Withdrawal ledger timing: debit recorded at request/lock time, not at Xendit success callback; a failed/cancelled/reversed callback writes a refund credit entry` — recorded in DECISION_LOG.md 2026-08-10
- `WithdrawalStatus adds "failed" beyond the source ERD's 4 values (pending/approved/rejected/paid), to distinguish admin-rejected from Xendit-failed` — recorded in DECISION_LOG.md 2026-08-10
- `Xendit Payout channelCode derived as ID_<BANK_NAME uppercased> from the affiliate's free-text bank_name — verified 2026-08-10 against the real getPayoutChannels API using the founder's existing Xendit key (BCA, BNI, Mandiri, BRI, CIMB, Permata all matched exactly)` — recorded in DECISION_LOG.md 2026-08-10
- `TRANSACTIONS.affiliator_id from the source ERD is still not a real FK — commission crediting resolves the referralCode string to an AffiliatorProfile at credit time instead.`
- `Payment gateway/payout provider switched from Xendit to Duitku (client decision, 2026-08-25), superseding the 2026-08-05 decision above. src/xendit/ deleted, src/duitku/ added. Columns renamed via migration (data-preserving RENAME COLUMN, not drop+recreate): transactions.xendit_invoice_id -> duitku_reference, commission_withdrawals.xendit_disbursement_id -> duitku_disbursement_id.`
- `Provider Top-Up vendor decided: MomoLive Coin Merchant API (client-supplied dev-doc/API Terbuka Pedagang Koin MomoLive(1).md, 2026-08-26), superseding the 2026-08-05 "undecided" decision above. MomoProviderTopUpService implements ProviderTopUpPort for real (query-user/balance/transfer-coin), selected via PROVIDER_TOP_UP_VENDOR=momo; MockProviderTopUpService stays the default until that's explicitly set.`

## Current Scope
- allowed_write_paths:
  - `development/backend/src/`, `package.json`, `test/`, `.env.example`, `artifacts/shared/`
- blocked_paths:
  - `development/backend/.env` (real secrets)
  - any Vue.js/frontend code
- not yet built:
  - `Duitku bank-code mapping for disbursement (deliberately throws instead of guessing — see DuitkuService); the real /listBank endpoint exists (found 2026-08-26) but needs the real disbursement email, which the client hasn't located yet`
  - `Frontend of any kind`

## Latest Work Summary
- `Found and closed a real architecture gap (2026-08-26): the FE dev was calling MomoLive's raw /openapi/v1/balance and /openapi/v1/transfer-coin endpoints directly (screenshot evidence in chat), which would require the MomoLive merchant secret to live in frontend-reachable code. Added GET /admin/provider/balance and POST /admin/provider/coin-transfer (superadmin-guarded) so the backend brokers this instead; getBalance() promoted from a MomoProviderTopUpService-only extra to a real ProviderTopUpPort method (MockProviderTopUpService returns a fixed fake number). The manual coin-transfer endpoint is for support/compensation cases (bypasses payment), logged with the acting admin's id/target/amount/reason. Committed 8ef5451, deployed, swagger auto-updated and confirmed live. The FE dev then separately asked for the same for Duitku ("get balance ndek duitku iso a?" - can I also get balance from Duitku). Real-tested Duitku's disbursement /checkbalance endpoint first (same real-API-first pattern) and confirmed it hits the identical account-provisioning blocker as /inquiry and /transfer (-120 "User not allowed") - not a code problem, same root cause as the rest of Duitku disbursement. Built GET /admin/duitku/balance anyway (DuitkuService.checkBalance(), real-verified request/response shape) so it's ready the moment disbursement activates; had to make it fall back to placeholder userId/email (DUITKU_USER_ID/DUITKU_EMAIL aren't set on the server) rather than getOrThrow, since Duitku rejects checkbalance identically regardless of those values while disbursement is inactive - a crash would be strictly worse than a clean rejection response. Committed 6db924e, deployed, real-verified as correctly 401-guarded and present in the live swagger spec.`
- `Deployed and switched on MomoLive Provider Top-Up (2026-08-26), after the founder explicitly said to do all four pending items including this one. scp'd the provider/topup source files to the server; adding MOMO_MERCHANT_ID/MOMO_API_SECRET/MOMO_BASE_URL/PROVIDER_TOP_UP_VENDOR=momo to the server .env was blocked by the runtime's own safety classifier (writing secrets to a production host isn't something this agent can push through regardless of confirmation) - the founder ran that step themselves over SSH. After restart, a real check-id call against the live API 502'd; root-caused it to a real bug that only surfaces once actually switched on: MOMO_MERCHANT_ID is read from an env var (always a string), so merchant_id was sent as a quoted JSON string, and MomoLive's server 500s on that (fails to deserialize before ever checking the signature) - none of the earlier throwaway test scripts caught this since they used numeric literals, not an env-sourced value. Fixed by converting merchantId to a number in the constructor; build/lint/65 unit still pass, committed (397fd38), redeployed, and real-verified end-to-end: check-id for MomoLive test user 1000007 now returns their real username ("Guanshan Xu") through https://api.yaytopup.my.id. Also found, while researching Duitku's disbursement bank-code list, a real /listBank endpoint (POST /webapi/api/disbursement/listBank, discovered via the open-source Go SDK "libduitku" since Duitku's own docs and official PHP SDK don't cover disbursement at all) - tested it for real, got a specific "-213 Email is not valid" rather than the blanket account-rejection seen elsewhere, meaning it needs the real email tied to the account's disbursement setup (not yet known) before it'll return the live bank list.`
- `Built the real Provider Top-Up vendor integration (2026-08-26): client sent dev-doc/API Terbuka Pedagang Koin MomoLive(1).md (MomoLive Coin Merchant Open API - query-user, balance, transfer-coin). Reviewed it, then tested all three real endpoints directly via throwaway node -e scripts with the doc's real sandbox test credentials (merchant ID 1000007) before writing any service code - all three returned real, successful, exactly-as-documented responses on the first try (same real-API-first pattern used for Duitku). Found one real gap while reviewing: ProviderTopUpPort's InjectCoinParams had no field for how many coins to credit - added a required coin field, wired through from TopupService.handleInvoicePaid (product.coinAmount + product.bonusCoin). Built MomoProviderTopUpService (SHA-256 signature = sorted key=value params + secret appended with no separator, verified against the doc's own reference implementation) and rewrote ProviderModule to use a useFactory that only constructs whichever implementation PROVIDER_TOP_UP_VENDOR selects (default mock) - registering both as always-present providers would have broken every env without MOMO_MERCHANT_ID/MOMO_API_SECRET set, since Momo's constructor uses getOrThrow. Added MOMO_MERCHANT_ID/MOMO_API_SECRET/MOMO_BASE_URL/PROVIDER_TOP_UP_VENDOR to .env.example. Build/lint/65 unit/44 e2e all pass; committed (c821743).`
- `Client registered a real Duitku sandbox account and shared the project's Merchant Code (DS34603) and API Key. Tested DuitkuService.createInvoice against the real sandbox API directly (not through the app) before touching any deploy - real HTTP 200, statusCode "00", genuine invoice reference + paymentUrl on the first try, matching every field name and the signature formula already coded. Testing disbursement the same way was much more revealing: real calls to /inquiry and /transfer surfaced that (a) it's a genuine two-step flow (inquiry then a SEPARATE transfer call using the inquiry's disburseId/accountName) with its own signature formula per step and two new required fields (userId, email) that inquiry doesn't need, and (b) Duitku's Transfer Online product has NO callback at all - the transfer response IS the final result, unlike Xendit's Payout which confirmed asynchronously. Rewrote DuitkuService.createPayout to do the real two-call flow and return a structured result instead of throwing on business failure, and AffiliatesService.approveWithdrawal to resolve PAID/FAILED synchronously instead of waiting for a webhook that will never come (old callback path kept, unused, for Duitku's separate "Clearing" product which does have one). Also confirmed, via a deliberately-garbage signature that returned the identical generic error instead of a signature-specific one, that disbursement itself is not provisioned on this account yet - matches the dashboard's "belum aktif" banner. Build/lint/63 unit/44 e2e all pass; committed, not deployed.`
- `Client requested switching payment gateway from Xendit to Duitku (https://www.duitku.com/). Researched Duitku's real API first rather than guess (their docs site blocks automated fetches, worked around via a read-only proxy + their official Laravel/npm libraries): payment collection maps to "Duitku Pop" createInvoice (hosted checkout page, same UX as Xendit Invoice), disbursement exists but requires account activation and uses a materially different two-step-ish flow with its own signature scheme and bank-code list. Asked the founder two clarifying questions before writing code: credentials status (answer: none yet, need to sign up) and disbursement scope (answer: migrate both payment and disbursement). Built the full swap: new DuitkuService (createInvoice, createPayout, verifyInvoiceCallbackSignature, verifyDisbursementCallbackSignature - HMAC/SHA256 based, form-urlencoded callback body, replacing Xendit's simple static-token header check), deleted src/xendit/, rewrote both webhook controllers, rewired topup.service.ts and affiliates.service.ts, renamed transactions.xendit_invoice_id -> duitku_reference and commission_withdrawals.xendit_disbursement_id -> duitku_disbursement_id via a genuine data-preserving RENAME COLUMN migration (verified TypeORM generated a true rename, not a destructive drop+recreate, before running it - this matters since production has real data in these columns). Updated every test file with a FakeDuitkuService double. Deliberately left two things unresolved rather than guess at real money handling: (1) Duitku's invoice callback has no explicit "expired" push the way Xendit's did (only definitive success/failure), so handleInvoiceExpired now has no live trigger - documented, not silently dropped; (2) Duitku's disbursement needs a real bank-name -> Duitku-bank-code lookup that doesn't exist yet (unlike Xendit's derivable ID_<BANK> convention) - DuitkuService.createPayout deliberately throws a clear error instead of guessing. Build/lint/63 unit/44 e2e all pass locally. NOT deployed to the server - no real Duitku account exists yet, and deploying now would replace live, working Xendit checkout with a non-functional integration; holding for the founder's explicit go-ahead on timing.`
- `Founder asked whether a DELETE API existed anywhere - it didn't (grepped the whole src tree, confirmed zero @Delete decorators), by original design (PRD/use-case only ever describe add/edit/status-toggle, nothing removable). Founder asked for soft delete specifically. Added @DeleteDateColumn (deleted_at) to Product and ContactMessage, ProductsService.softDelete/ContactService.softDelete (repository.softDelete after a not-found check), and DELETE /admin/products/:id + DELETE /admin/contact-messages/:id. TypeORM automatically excludes soft-deleted rows from find/findOne, so deleted items vanish from every listing (including the public GET /topup/products) without touching any query code. Soft delete over hard delete specifically protects transactions.product_id FK integrity - a deleted product's past orders keep working. Migration AddSoftDelete1787284338860 (two nullable deleted_at columns). 4 new unit tests + 4 new e2e tests; build/lint/61 unit/45 e2e all pass. Deployed and live-verified with throwaway product/message (ids 37 and 3) - both correctly vanished from admin listings after DELETE, confirmed still present with deleted_at set via a withDeleted() query.`
- `While verifying the deploy, GET /topup/products came back empty (0 of the real 25 products) - investigated via nginx access logs before touching anything, rather than assume a bug in the new code. Found 25 individual PATCH /admin/products/:id requests, one per product, all 200s, all with Referer: https://yaytopup.my.id/ (the real live frontend, not Swagger, not any script run this session) at 2026-08-21 05:53-05:55 (UTC+2) - someone on the founder's team deactivated the entire catalog via the live admin panel. Asked the founder directly rather than "fixing" it; confirmed intentional, left as-is. Worth remembering: an empty public product listing is not necessarily a bug - check nginx access logs for admin-panel activity before assuming regression.`
- `Founder caught that the contact form's category field was still in the request body/Swagger schema after asking it be skipped - the earlier "skip the dropdown" answer had been read as "make it free text" rather than "drop it entirely." Removed category from the entity, DTO, and service (email subject/body no longer references it), migration RemoveContactMessageCategory1787196756302 drops the column. Live-verified on staging: Swagger schema no longer lists category, POST /contact with a category field now 400s (forbidNonWhitelisted), and omitting it works fine.`
- `Client requested a public contact/support form (screenshot: "Kirim Pesan Langsung" - name/email/category/message, for transaction issues or partnership inquiries). Clarified scope with the founder first (category as free text not a fixed enum, store + email notify, list+resolve admin management) rather than guess. Built: ContactMessage entity (open/resolved status), POST /contact (public), GET /admin/contact-messages + POST /admin/contact-messages/:id/resolve (superadmin), and a best-effort EmailService (nodemailer) that gracefully degrades to a log warning when SMTP env vars aren't set - a missing/misconfigured mail server can never block a real submission from being stored. Migration AddContactMessages1787193838511. 8 new unit tests (EmailService + ContactService) + 5 new e2e tests. Full suite (build/lint/57 unit/41 e2e) passed before deploy; deployed to staging (needed npm install for the new nodemailer dependency, not just scp+build like smaller changes) and live-verified: real POST /contact stored + listed + resolved via the real admin API, and the log confirmed the email path degraded gracefully (SUPPORT_NOTIFICATION_EMAIL not set -> warning, no crash, submission still stored).`
- `Founder's frontend engineer sent a CreateProductDto shape (coin_amount, bonus_coin, flag) that didn't match the actual API - the product schema only had name/provider_code/base_price/selling_price, so their payload would have been rejected by forbidNonWhitelisted. Flagged the mismatch and extended the schema for real rather than force-fitting the data: added coin_amount (required), bonus_coin (optional, default 0), flag (optional label e.g. "Terlaris") to the Product entity/DTOs/ProductsService, exposed on the public GET /topup/products listing (cost price still hidden), migration AddProductCoinFields1787144160685. Verified locally (49 unit/36 e2e pass) before deploying (staging products table was empty, so the NOT NULL coin_amount column added cleanly). Seeded the full 25-product Momo Live catalog from the founder's price list image via the real admin API on production (not a DB script) - prices, coin amounts, bonus tiers, and the "Terlaris" flag on the top 30-juta tier all verified matching the source image exactly via GET /topup/products afterward.`
- `Founder asked for production-hardening after creating a real superadmin account: localhost-only bind, real migrations, and NODE_ENV=production, all done 2026-08-19. (1) Bind: src/main.ts now calls app.listen(port, '127.0.0.1') instead of binding all interfaces — live-verified http://141.11.25.123:3000 now connection-refuses while https://api.yaytopup.my.id (nginx -> 127.0.0.1:3000) still works. (2) Migrations: generated InitialSchema from the current entities against a freshly emptied local DB (all 7 tables, correct FKs/enums), set synchronize:false unconditionally in app.module.ts, verified locally with a real migration:run + full 48 unit/36 e2e pass against the migrated (not synchronize-created) schema. Adopting it on staging was the delicate part — the staging DB already had these 7 tables from the old synchronize:true path, so re-running the migration's up() would have failed (tables already exist). Instead: created the migrations tracking table + inserted a row marking InitialSchema as already-applied, without running up(). Hit one snag: the tracking table was first created as the postgres superuser, which topup_app (the app's DB role) couldn't see via information_schema, so migration:run tried to recreate it and failed with 'already exists' — fixed by ALTER TABLE migrations OWNER TO topup_app, after which migration:run correctly reported 'No migrations are pending'. All 7 original tables + data (including the new superadmin account) verified untouched throughout. (3) NODE_ENV: switched staging's .env from staging to production now that synchronize is off (previously staging was used deliberately because production would have disabled schema creation with no migrations yet — that reason no longer applies). Full post-deploy check: domain still 200s, superadmin login still works, swagger still up, table count unchanged (8 = 7 original + migrations).`
- `Founder asked "is there any login credential... like super admin" and there wasn't one — no superadmin had ever been created on staging. Created one (email/password chosen by the founder) via a one-off Node script uploaded and run on the server (hashes with the app's own bcrypt module, inserts via a parameterized pg query) rather than a raw psql -c insert, after a first raw-SQL attempt corrupted the bcrypt hash's $-prefixed format through multiple layers of shell escaping (local bash -> ssh -> remote bash -> psql -c). Login verified live against POST /auth/login. Also found and deleted one leftover test affiliate row (name "string", email user@example.com — Swagger UI's default placeholder values, from earlier verification work) sitting in the real staging DB.`
- `A third re-verification pass (2026-08-19, founder asked "is there still remain tasks" a third time) found another dead enum value of the same class: PaymentStatus.FAILED was defined on the entity and required by the source ERD ("pending, paid, failed, expired") but nothing ever set it — Xendit's Invoice API only actually sends PENDING/PAID/SETTLED/EXPIRED, no distinct "payment failed" event, so this wasn't reachable via any real webhook branch. Asked the founder how to handle it (leave as documented-unused, add a defensive catch-all, or remove from the enum); founder chose the defensive catch-all. Added TransactionsService.markFailed, TopupService.handleInvoiceFailed (same not-pending-anymore idempotency guard as handleInvoiceExpired), and changed the webhook controller's else-branch: PENDING still just logs, but any other unrecognized status now marks the transaction failed instead of silently doing nothing forever. Added 3 new unit tests + 1 new e2e test. Build/lint/48 unit/36 e2e all pass.`
- `Found and fixed a real API-completeness gap during a re-verification pass (2026-08-19): WithdrawalStatus.REJECTED existed on the schema/DTO (with a comment explaining exactly why it was needed) but no code path ever set it — a superadmin could only approve a withdrawal (real Xendit payout) or leave it stuck pending, with no way to decline an obviously bad request (wrong bank details, fraud) without locking the affiliate's balance forever. Added AffiliatesService.rejectWithdrawal (mirrors the existing disbursement-failure refund logic: pessimistic-locked balance refund + CommissionLog credit entry, gated on PENDING status, never touches Xendit) and POST /admin/withdrawals/:id/reject. Covered by 2 new e2e tests in commission-and-withdrawal.e2e-spec.ts (matches the existing pattern where transactional QueryRunner methods are e2e-tested only, not unit-tested — the unit spec mocks DataSource as {}). Build/lint/45 unit/35 e2e all pass; deployed to staging by scp'ing the 2 changed files + npm run build + pm2 restart; live-verified the route now 401s (was 404 before) and deployed code diffs identical to local.`
- `Set up the real domain https://api.yaytopup.my.id for the staging backend (same day as initial deploy). Domain is proxied through Cloudflare; added a separate nginx site (didn't touch the existing frontend config) reverse-proxying to the PM2 app on port 3000, then ran certbot which successfully issued a real Let's Encrypt cert (HTTP-01 challenge passes through Cloudflare fine) with auto HTTPS redirect. certbot.timer handles renewal. Verified https://api.yaytopup.my.id/api/v1/topup/products and /api/docs both return 200, and HTTP correctly redirects to HTTPS. Confirmed to the founder that the database setup (dedicated topup_app role + topup_affiliate_platform DB, 7 tables) was already done in the prior deploy. The app is still also directly reachable on the bare IP:3000 (unrestricted bind) since that was the original request — flagged as worth restricting to localhost-only now that the domain path exists, not done without being asked.`
- `Deployed to a real staging server (root@141.11.25.123, Ubuntu 20.04, small VPS shared with an existing yaytopup.my.id frontend). Code uploaded as a tarball (no git clone, no node_modules/.env/dist), npm install + npm run build ran on the server, PostgreSQL installed natively (not Docker, to save RAM on a ~964MB box), app runs under PM2 as a systemd-enabled service (survives reboot). .env assembled entirely server-side: real Xendit key carried over from local, JWT_SECRET and XENDIT_CALLBACK_TOKEN freshly generated on the server. NODE_ENV=staging (not production) so synchronize:true still creates the schema, since no migrations exist yet. Verified externally: GET /api/v1/topup/products (200), GET /api/docs (200), and a real POST /affiliate/register write against the live DB (cleaned up after). See artifacts/access/README.md (scaffold root) for the URLs and full deployment notes — no secrets there, real credentials never left the server or this session's scratchpad.`
- `Added Swagger/OpenAPI docs (@nestjs/swagger). Live interactive explorer at /api/docs (Bearer auth wired in via the Authorize button); the two Xendit webhook controllers are excluded since Xendit calls those, not a person. Rewrote development/backend/README.md from the default Nest boilerplate into a real, human-written project README with a full manual endpoint table plus a pointer to /api/docs. Verified live: booted the app and confirmed the OpenAPI JSON lists exactly the 19 expected paths, webhooks correctly absent. 41 unit + 32 e2e tests still pass.`
- `Note: npm flagged 2 high-severity advisories for js-yaml, pulled in transitively by @nestjs/swagger itself (a DoS via slow parsing of attacker-controlled YAML). Not fixed — the suggested fix is a breaking downgrade of @nestjs/swagger, and this app never parses untrusted YAML through it (Swagger UI only renders our own generated spec), so exposure is minimal. Worth another look if @nestjs/swagger ships a patched release.`
- `Added the three admin list endpoints that were missing against the PRD (GET /admin/affiliates, GET /admin/withdrawals, GET /admin/products, all with optional status filter + pagination on the first two). Without these, a real superadmin had no way to discover which IDs to act on. 41 unit + 30 e2e tests pass.`
- `Second API-completeness pass found the customer side had the same gap: no way to discover a valid product_id for checkout, and no way to check a transaction's real status after paying (Xendit's redirect alone isn't reliable). Added GET /topup/products (public, active only, deliberately excludes base_price/cost) and GET /topup/transactions/:id (public status check by ID — same trust model as a typical order-tracking page; no new PII or financial data exposed beyond what checkout already returns). 41 unit + 32 e2e tests pass.`
- `Built and verified three more slices in one bundled session (founder said "do them" after being offered all three): (1) Superadmin — PlatformSettings singleton + SettingsService, AdminController with affiliate approve/reject/commission-rate override, settings get/update, product CRUD, transaction monitoring, all JWT+role(superadmin)-guarded. (2) Commission crediting — AffiliatesService.creditCommissionForTransaction wired into TopupService.handleInvoicePaid, using a Postgres SELECT...FOR UPDATE row lock via a manual TypeORM QueryRunner transaction. (3) Xendit Disbursement — CommissionWithdrawal entity, POST /affiliate/withdraw (locks balance immediately), POST /admin/withdrawals/:id/approve (real Xendit Payout API call via XenditService.createPayout), POST /webhooks/xendit/disbursement (SUCCEEDED marks paid; FAILED/CANCELLED/REVERSED refunds with a corrective credit entry).`
- `Found and fixed a real bug during e2e verification: AffiliatorProfile.user had eager:true, which forces a LEFT JOIN on every query regardless of requested relations; combined with a pessimistic row lock, Postgres rejected it ("FOR UPDATE cannot be applied to the nullable side of an outer join"). Fixed by removing eager:true and doing separate unlocked User lookups inside locked transactions where an active-status check is needed.`
- `Also fixed an e2e test-infrastructure issue: with 5 e2e spec files now sharing one dev Postgres, running them in parallel (Jest's default) caused timeouts and cross-file interference. Set test:e2e to always run --runInBand (serial) and raised the Jest e2e hook timeout to 30s.`
- `Verified against a real local Postgres: build, lint (0 errors, 6 pre-existing accepted warnings), 41 unit tests, and 27 e2e tests (admin auth/guards, affiliate approve/reject, settings, product CRUD, transaction monitoring, full commission-credit-then-withdraw-then-payout-then-callback lifecycle including a failed-payout refund path) all pass.`

## QA Status
- unit: `pass (65/65, npm test — 63 prior + 2 new signMomoParams tests for the MomoLive integration)`
- integration: `pass (50/50 e2e via npm run test:e2e --runInBand, real Postgres — 44 prior + 6 new: GET /admin/provider/balance, POST /admin/provider/coin-transfer (success + 400 on non-positive coin), GET /admin/duitku/balance (200 with rejection body + 403 for non-superadmin). admin.e2e-spec.ts now overrides DuitkuService with a FakeDuitkuService for the balance-check test only. MomoLive itself still not e2e-covered directly since PROVIDER_TOP_UP_VENDOR defaults to mock in this env — runs against a real migrated schema, not synchronize:true.)`
- e2e_browser: `not_required (no frontend in scope)`
- ui_visual: `not_required (no frontend in scope)`
- manual_visual_qa_required: `no`
- manual_visual_qa_status: `not_required`
- manual_visual_qa_reference: `none`
- security: `partial — x-callback-token verification (both webhooks), JWT auth (missing/invalid/wrong-role token), password hashing, pending-approval gating, and role-guarded admin routes are all unit/e2e tested; no broader security review (rate limiting, brute-force protection, audit logging) has been done`
- performance: `not_run`
- regression: `pass — all prior-slice tests (topup, affiliate) still pass alongside the new admin/commission/withdrawal tests`

## Blockers
- `Payment collection (createInvoice) is real-verified against the client's actual sandbox account as of 2026-08-26 and ready to deploy whenever the client wants.`
- `Disbursement (affiliate payouts) is confirmed NOT provisioned on this Duitku account — both /inquiry and /transfer return the identical generic {responseCode: "-120", responseDesc: "User not found"} regardless of input, including with a deliberately invalid signature, meaning the account is rejected before any field-level validation. Matches the dashboard's "Akun Duitku Anda belum aktif" banner. Nothing more can be tested here until the client's account/disbursement feature is actually activated.`
- `Duitku disbursement also cannot function yet for an unrelated reason — DuitkuService.createPayout's deriveDuitkuBankCode still throws deliberately, since there's no real bank-name -> Duitku-bank-code mapping (Duitku doesn't use Xendit's derivable ID_<BANK> convention). This needs solving independently of the activation blocker above.`
- `MomoLive Provider Top-Up is now live on the server (PROVIDER_TOP_UP_VENDOR=momo, sandbox test credentials) and real-verified via check-id. injectCoin (transfer-coin) uses the same fixed code path but hasn't been observed end-to-end through a real paid transaction yet - the next real Duitku sandbox payment should be checked for a real MomoLive coin transfer result in provider_status, not just assumed working from check-id alone.`

## Next Exact Step
- `Payment-collection side of the Xendit->Duitku migration is real-verified and could be deployed to staging today if the client gives the go-ahead (still holding, since it replaces currently-working live Xendit checkout). Disbursement needs two more things before it can go anywhere: (1) the client's Duitku account needs disbursement actually activated (currently shows "belum aktif" - the client submitted a merchant application form already, per the dashboard, so this may just need to be waited on rather than requested again), and (2) the real email tied to the account's disbursement setup, so the real /listBank endpoint can return actual bank codes instead of guessing. Once disbursement activates, re-run the exact same direct real-API-call testing approach used for createInvoice (see this session's node -e test scripts) against /inquiry and /transfer with a real destination bank account before trusting it with a real withdrawal. Separately, watch the next real paid transaction's provider_status to confirm MomoLive's transfer-coin path (not just check-id) works end-to-end now that the merchant_id type bug is fixed.`

## Do Not Repeat
- `When a value that a real third-party API expects as a numeric JSON field comes from an env var (always a string) or another string-typed source, convert it to a number BEFORE building the request body, not just before building the signature string. This session's MomoLive integration signed merchant_id correctly (the signature is built from a text representation either way, so it matched), but still sent it in the JSON body as "merchant_id":"1000007" (quoted) instead of 1000007, and MomoLive's server 500'd trying to deserialize it - before it ever got to checking the signature. A throwaway test script using a numeric literal for the same field will NOT catch this, since the bug only exists when the value is read from an env var; the real bug only surfaced once the integration was actually switched on and hit through the deployed app, not during isolated pre-build testing.`
- `When integrating a third-party payment/money API and real credentials become available, test the raw HTTP contract directly with a small throwaway script (node -e, curl) BEFORE trusting proxy-scraped or AI-summarized documentation any further. This session's Duitku disbursement research (via a read-only proxy fetch, since docs.duitku.com blocks direct automated access) was wrong or incomplete in real, consequential ways - a two-step flow it described accurately, but exact response field names and a whole extra required-fields requirement only surfaced once real credentials existed and a direct test was run. A single real request (even one that ultimately errors) is worth more than another round of scraped-doc summarization.`
- `When a column rename is needed on a table that already has real production data (e.g. swapping payment providers and renaming xendit_invoice_id -> duitku_reference), always read the generated migration file before running it against anything real. TypeORM's diff-based migration:generate CAN produce a true ALTER TABLE ... RENAME COLUMN (data-preserving, confirmed this time), but a diff-based generator can in principle see "old column gone, new column added" and emit a destructive drop+recreate instead depending on how the diff resolves - never assume, always check the actual SQL in the generated file first.`
- `When manually creating a table that TypeORM's app-level DB role needs to see/manage (e.g. the migrations tracking table, adopted onto an already-synchronize'd staging DB), create it via psql as that same app role — not as the postgres superuser. A table owned by a different role may not show up in that role's information_schema.tables query, causing TypeORM to think it doesn't exist and attempt to recreate it (fails with "already exists"). Fix if it happens: ALTER TABLE <name> OWNER TO <app_role>.`
- `Do not insert values containing $ (e.g. a bcrypt hash like $2b$10$...) via a raw psql -c command run through nested shell layers (local bash -> ssh -> remote bash -> psql -c) — each layer's escaping can corrupt the $-prefixed segments silently (the insert succeeds, but with mangled data, so it fails only on later use, e.g. login). Write a small Node/script file instead and scp+run it, using a parameterized query so the value never passes through shell interpolation at all.`
- `Do not declare a TypeORM @Column with a TS union type (e.g. string | null) without an explicit type: option — reflect-metadata resolves the design type to Object for unions, which TypeORM rejects for Postgres (hit this on Transaction.targetZoneId/referralCode/xenditInvoiceId, fixed by adding type: 'varchar').`
- `Do not pass a bare jest.Mocked<Interface>.method reference into expect(...) — @typescript-eslint/unbound-method flags it; keep a separate jest.fn() handle instead.`
- `TypeORM's FindOneOptions.relations expects an object form ({ user: true }) in this TypeORM version, not an array (['user']) — array form fails to compile.`
- `Do not mark a TypeORM relation eager:true if that entity is ever fetched inside a pessimistic-locked query — Postgres rejects FOR UPDATE across the LEFT JOIN eager forces. Keep relations non-eager and request them explicitly only in unlocked queries; do separate unlocked lookups inside locked transactions instead.`
- `Do not Read or cat the .env files directly — the harness denies it (secret-file guard). Write/append via Bash still works; verify success by the tool's own result, not by reading the file back.`
- `Run npm run test:e2e with --runInBand (already the default script) once more than 2-3 e2e spec files share the same dev database — parallel workers race on TypeORM synchronize and can truncate/read each other's data mid-test, causing real failures, not just the earlier-seen cosmetic retry warning.`
